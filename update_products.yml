name: Mise à jour jadeship_products.json

on:
  schedule:
    - cron: '0 8 * * *'        # tous les jours à 10h00 Paris (08:00 UTC)
  workflow_dispatch:

jobs:
  update-json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Lancer le scraper
        # si vous avez défini un secret AFFIL_CODE → passez-le
        env:
          AFFIL_CODE: ${{ secrets.AFFIL_CODE }}
        run: python scraper/run_scraper.py

      - name: Commit & push
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add jadeship_products.json
          git commit -m "🛠️ Mise à jour titres et IDs – $(date +'%Y-%m-%d')"
          git push
